apiVersion: v1
kind: Service
metadata:
  name: hashicorp-service-loadbalancer
spec:
  type: LoadBalancer
  selector:
    app: hashicorp-echo
  ports:
  - protocol: TCP
    port: 80
    targetPort: 9999
