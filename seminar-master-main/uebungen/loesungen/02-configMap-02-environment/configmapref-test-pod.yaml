apiVersion: v1
kind: Pod
metadata:
  name: configmapref-test-pod
spec:
  containers:
    - name: configmapref-test-container
      image: busybox:1.31.0
      command: [ "/bin/sh", "-c", "env | grep firma" ]
      envFrom:
      - configMapRef:
          name: firma-config
  restartPolicy: Never

