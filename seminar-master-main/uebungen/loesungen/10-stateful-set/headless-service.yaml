apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  clusterIP: None                # ← macht ihn „headless“
  selector:
    app: nginx
  ports:
    - name: web
      port: 80
      targetPort: 80
